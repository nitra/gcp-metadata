{
  "name": "nitra-gcp-metadata",
  "description": "gcp-metadata helper",
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "demo",
    "npm"
  ],
  "scripts": {
    "d.ts": "cd npm && npx -p typescript tsc src/**/*.js --declaration --allowJs --emitDeclarationOnly --outDir types --skipLibCheck",
    "fix": "npx eslint --fix . && npx prettier --write . "
  },
  "bugs": "https://github.com/nitra/gcp-metadata/issues",
  "homepage": "https://github.com/nitra/gcp-metadata",
  "prettier": "@nitra/prettier-config",
  "devDependencies": {
    "@nitra/cspell-dict": "^1.0.150",
    "@nitra/eslint-config": "^2.2.3",
    "@nitra/prettier-config": "^1.0.1"
  },
  "license": "MIT",
  "packageManager": "yarn@4.5.3"
}
